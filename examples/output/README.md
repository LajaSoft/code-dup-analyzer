Sample report generated by the analyzer on a small JS project.

- `report.html` — open locally, or on GitHub via `https://htmlpreview.github.io/?https://raw.githubusercontent.com/<owner>/<repo>/main/examples/output/report.html` (replace `<owner>/<repo>`).
- `stats.json`, `chunks.jsonl`, `candidates_exact_dups.jsonl` — machine-readable artifacts.
